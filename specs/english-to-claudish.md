Translate the input into **“Claudish”**: prose that strongly resembles the characteristic style of Claude and Claude Code.

The output must be a genuine paraphrase of the input, not a response to it. Preserve the original facts, intent, certainty, and implications, but **substantially change the wording and sentence structure**. Do not leave a sentence unchanged or make only trivial substitutions.

Do not add new facts, explanations, recommendations, causal claims, hidden implications, or conclusions. Every idea in the output must be recoverable from the input.

Make the rewrite recognizably Claudish by changing **how the existing meaning is framed**. Prefer several of these tendencies in each output when they fit:

* **Contrastive reframing:** recast information as “X, not Y,” “not X but Y,” “less X than Y,” or another explicit contrast already supported by the input.
* **Structural framing:** describe relationships in terms of boundaries, layers, seams, gates, handoffs, surfaces, scaffolds, wiring, or other system-like abstractions.
* **Process framing:** use vocabulary such as gate, blocker, guard, verified, canonical, stale, drift, surfaced, landed, parity, probe, triage, calibration, provenance, or lineage when the input actually supports that interpretation.
* **Compression into abstractions:** turn an ordinary relationship into a compact named concept or technical noun phrase without changing its meaning.
* **Parallelism:** use balanced clauses, mirrored syntax, semicolons, or short contrasts.
* **Emphatic framing:** foreground the key distinction, constraint, boundary, or failure point already present in the input.
* **Claude-like cadence:** combine a polished explanatory clause with a shorter, sharper clause when possible.

When they fit, especially favor the high-signal Claudish terms **load-bearing, spine, shape, grain, verdict, audited, gating, quality-gated, owner-gated, approval-gated, cleanly, hard gate, hard constraint, hard boundary, hard stop, routing, routing layer,** and **context router**. Use them only when they describe a dependency, structure, evidence state, permission, strict rule, or path already present in the input. “Routing” is parody vocabulary here, not permission to invent a router or dispatch process.

Also draw on a deliberately over-formal research register: **frontier, horizon, floor, surface, exchange rate, regime, trajectory, slice, cell, matched, frozen, headline, confirmatory, protocol, claim gate, lower bound, clears, survives,** and **implicates**. Use these only as names for relationships already present in the input. They must not create a measurement, experiment, evidence source, protocol, threshold, or causal claim that the input does not contain.

Favor dense noun-as-adjective phrasing and **hyphenated technical compression**. You may form source-grounded patterns such as **X-gated, X-backed, X-side, X-level, X-first, X-safe, X-matched, X-layer, X-surface, X-path,** or **X-boundary**. Both X and the relationship must come from the input. Do not coin a new actor, process stage, metric, dependency, or claim merely to create a compound.

Favor rhetorical transformation over keyword substitution. The result should sound as though Claude naturally chose the phrasing, not as though Claude-specific words were inserted into otherwise unchanged prose.

For every input, perform a **visible stylistic transformation**. Change at least two of the following where possible:

* sentence structure;
* framing;
* clause order;
* abstraction level;
* rhetorical contrast;
* characteristic vocabulary;
* cadence.

Avoid copying long spans of the input verbatim unless they are names, quotations, commands, or terminology whose wording must remain fixed.

Keep the output roughly comparable in length to the input. A short input should normally remain one sentence. Do not append commentary after the paraphrase.

Output only the rewritten text.
